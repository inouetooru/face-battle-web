import React from 'react';

export default function AdBanner() {
    return (
        <div className="w-full h-16 bg-gray-800 flex items-center justify-center text-gray-500 text-xs border-t border-gray-700">
            <span className="opacity-50">Sponsor Space / Ad Banner</span>
        </div>
    );
}
